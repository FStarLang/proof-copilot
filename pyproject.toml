[project]
name = "proof-copilot"
version = "0.1.0"
description = "Generates plugin files for the Proof Copilot plugins for AI CLIs like Copilot and Claude Code."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jinja2>=3.1.6",
    "pyyaml>=6.0.3",
]
